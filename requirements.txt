# Core agent dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional: For LangGraph formalization
langgraph>=0.0.20
langchain>=0.1.0

# Optional: For vector memory (embeddings)
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.24.0

# LLM providers (optional, for production)
openai>=1.0.0
